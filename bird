% Facts about birds and whether they can fly
bird(sparrow).
bird(eagle).
bird(crow).
can_fly(sparrow).
can_fly(eagle).

% Query to determine if a bird can fly
can_fly_bird(Bird) :-
    bird(Bird),
    can_fly(Bird).

% Query for the provided birds
?- can_fly_bird(sparrow).
?- can_fly_bird(penguin).
?- can_fly_bird(eagle).
?- can_fly_bird(crow).

